package components
{
    public class TimedEffect implements Component
    {
        // effectType constants found in utils.EffectTypes
        public var effectType:String;
        public var secondsRemaining:Number;
    }
}
